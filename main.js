const modelViewer = document.querySelector("model-viewer"); console.log("modelViewer : " + modelViewer); const fileInput = document.getElementById('textureSelector'); fileInput.addEventListener('change', async (event) => {   const files = event.target.files;   const firstFile = files[0];   const texture = await modelViewer.createTexture(URL.createObjectURL(firstFile));   const material = modelViewer.model.getMaterialByName('PosterMaterial');   material.pbrMetallicRoughness.baseColorTexture.setTexture(texture); });  document.getElementById("uploadButton").addEventListener('click', uploadFileClicked);  function uploadFileClicked(){   document.getElementById('textureSelector').click(); }  function arButtonClicked(){   document.getElementById("ar-button").click(); } 
